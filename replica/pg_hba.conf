# Production replica - allow local and Docker network
local   all             all                                     trust
host    all             all             0.0.0.0/0               scram-sha-256
